

const TypeMessage = ({message} : {message: string}) => {
  return (
    <div style={{ marginLeft: '10px', fontSize: '10px', fontWeight: 700, color: 'red', textAlign: 'start' }} >{message}</div>
  )
}

export default TypeMessage